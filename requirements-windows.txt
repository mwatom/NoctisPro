Django>=5,<6
Pillow>=10,<11
django-widget-tweaks
python-dotenv
celery>=5,<6
redis
psycopg2-binary; platform_system != 'Windows'  # avoid on Windows unless PostgreSQL dev libs present
django-redis
djangorestframework
django-cors-headers
# channels stack (optional, Windows-friendly)
channels>=4,<5
channels-redis>=4,<5
# ASGI server (we use waitress for WSGI; daphne only if needed)
# daphne>=4,<5

# DICOM
pydicom
pynetdicom
pylibjpeg
pylibjpeg-libjpeg
pylibjpeg-openjpeg
# gdcm (skip on Windows due to wheels availability)
# gdcm
highdicom

# Imaging and scientific (Windows-friendly versions)
opencv-python==4.8.1.78  # optional, can be skipped if not needed
scikit-image>=0.22,<0.23
matplotlib>=3.8,<3.9
numpy>=1.24,<2
scipy>=1.10,<1.12
pandas>=2.0,<2.2
scikit-learn>=1.3,<1.5

# Transformers (optional, heavy; comment out if not needed)
# transformers
# torch
# torchvision

# Docs and office
PyMuPDF
python-docx
openpyxl

# Security
cryptography
PyJWT

# Utilities
python-magic-bin==0.4.14  # use Windows binary build
requests
urllib3
qrcode

# Production server
waitress>=2,<3

dj-database-url